import { TextFilterDirective } from './text-filter.directive';

describe('TextFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new TextFilterDirective();
    expect(directive).toBeTruthy();
  });
});
